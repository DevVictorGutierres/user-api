# API de Usuários

Esta é uma API RESTful desenvolvida com **Node.js**, **Express** e **MongoDB** para gerenciamento de usuários. Ela permite criar, listar, buscar, atualizar e deletar usuários. A documentação dos endpoints está disponível via **Swagger**.

## 🚀 Funcionalidades

- `GET /api/users` — Lista todos os usuários
- `GET /api/users/:id` — Busca um usuário por ID
- `POST /api/users` — Cria um novo usuário
- `PUT /api/users/:id` — Atualiza um usuário existente
- `DELETE /api/users/:id` — Remove um usuário

## 🛠 Tecnologias utilizadas

- Node.js
- Express
- MongoDB + Mongoose
- Swagger (OpenAPI)
- dotenv

## 📦 Instalação

```bash
git clone https://github.com/seu-usuario/api-usuarios.git
cd api-usuarios
npm install
